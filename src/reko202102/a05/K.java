package aufgabe005;

/**
 * Die Methode hashCode einer Klasse K ist so implementiert, dass sie für
 * alle Objekte dieser Klasse denselben Hash-Code liefert.
 * Entscheiden Sie sich für eine der folgenden Antworten und begründen Sie sie.
 * 
 * Wenn Sie sich für die erste Antwort entscheiden, müssen Sie klar begründen,
 * was in diesem Fall nicht funktioniert oder welcher Fehler auftritt.
 * 
 * Wenn Sie sich für die zweite Antwort entscheiden, müssen Sie klar begründen,
 * worin sich der Nachteil dieser Implementierung zeigt.
 * 
 * ﾰ Die Implementierung ist nicht zulässig.
 * 
 * ﾰ Die Implementierung ist zulässig, aber ungünstig.
 *
 */
public class K {
    
}